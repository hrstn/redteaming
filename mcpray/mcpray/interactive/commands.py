# Superseded by the commands/ package directory (Python prefers the package).
# This file is kept only as a fallback reference.
from mcpray.interactive.commands import COMMANDS as COMMANDS  # noqa: F401
